package com.electrofear.misc;

/* Parses Map.xml
 * Creates a MapObject and returns
 * Also, adds to world object if it finds anything map specific
 */
public class MapParser {
	
	public MapObject parseMap(String inputMapName){
		MapObject myMapObject = new MapObject();
		
		
		return myMapObject;
	}
}
